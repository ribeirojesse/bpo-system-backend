Criar migration
alembic revision --autogenerate -m "mensagem"

Aplicar migrations
docker exec -it bpo_api alembic upgrade head

Voltar migration
alembic downgrade -1


IMPORTANTE

Sempre que criar novos models:

from app.models.user import User
from app.models.client import Client

Precisam ser importados no:

app/models/__init__.py

E também no:

alembic/env.py

Senão o Alembic não detecta as tabelas.